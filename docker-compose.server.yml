# VPS backend-only deploy
# Ishlatish:
#   cd /home/deploy/school-attendance-backend
#   docker compose -f docker-compose.server.yml up -d --build

version: '3.8'

services:
  # ── PostgreSQL ──────────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: school_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: school_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - school_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 768M
          cpus: '0.8'
        reservations:
          memory: 256M

  # ── Redis ───────────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: school_redis
    restart: always
    networks:
      - school_net
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.3'
        reservations:
          memory: 32M

  # ── Backend (NestJS) ────────────────────────────────────────────────────────
  backend:
    build:
      context: .
    container_name: school_backend
    restart: always
    ports:
      - "3001:3001"
    env_file:
      - .env.prod
    environment:
      DATABASE_URL_PROD: postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/school_db?schema=public
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/school_db?schema=public
      REDIS_HOST: redis
      REDIS_PORT: 6379
      PORT: 3001
      NODE_ENV: production
      TZ: Asia/Tashkent
      NODE_OPTIONS: --max-old-space-size=512
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - school_net
    deploy:
      resources:
        limits:
          memory: 900M
          cpus: '1.5'
        reservations:
          memory: 300M

networks:
  school_net:
    driver: bridge

volumes:
  postgres_data:
